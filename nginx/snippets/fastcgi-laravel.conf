# Один набор: FastCGI → Laravel public/index.php + таймауты (без дублирования в server{})
include snippets/fastcgi-laravel-base.conf;

fastcgi_connect_timeout 60s;
fastcgi_read_timeout 300s;
fastcgi_send_timeout 300s;
