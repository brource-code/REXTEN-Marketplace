/**
 * Иконки не задаём здесь: Next сам выставляет ссылки из `src/app/favicon.ico`,
 * `icon.svg`, `apple-icon.svg` (с digest в проде). Дубли с «голым» /favicon.ico
 * оставляли старый кэш таба при том же URL.
 */
const pageMeta = {
    title: 'REXTEN — Платформа для управления бизнесом',
    description:
        'Платформа для управления бизнесом с бронированиями, заказами и клиентами',
}

export default pageMeta
