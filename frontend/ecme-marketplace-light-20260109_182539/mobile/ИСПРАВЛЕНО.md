# ✅ Проблема исправлена!

## Что было сделано:

1. ✅ Установлен `babel-preset-expo`
2. ✅ Исправлена конфигурация `babel.config.js`
3. ✅ Очищен кэш

## 🚀 Теперь перезапустите Expo:

```bash
cd "/Users/turbin/Desktop/REXTEN marketplace/ecme-admin/mobile"
npx expo start --clear
```

Флаг `--clear` очистит кэш Metro bundler.

---

## Если все еще не работает:

Попробуйте полную переустановку зависимостей:

```bash
cd "/Users/turbin/Desktop/REXTEN marketplace/ecme-admin/mobile"
rm -rf node_modules
npm install
npx expo start --clear
```

---

## ✅ Готово!

После перезапуска Expo должен запуститься без ошибок.

